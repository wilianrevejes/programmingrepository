// ex003 - Crie um programa que leia dois números e mostr a soma entre eles.

const readline = require('readline');
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});


let n1 = readline();

console.log(`Teste de n1: ${n1}`);

